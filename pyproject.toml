[project]
name = "mpremote-sync"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ambadran", email = "ambadran717@hotmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
mpremote-sync = "mpremote_sync:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
